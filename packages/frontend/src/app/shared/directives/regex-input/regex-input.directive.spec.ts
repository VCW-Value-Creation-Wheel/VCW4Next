import { RegexInputDirective } from './regex-input.directive';

describe('RegexInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RegexInputDirective();
    expect(directive).toBeTruthy();
  });
});
